import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customers',
  description: 'The Customers Page that never got built.',
};

export default function Page() {
    return <p>Customers Page</p>;
}